# Influencer Campaign Tracker

This project is a Django web application for managing influencer campaigns.
Users can add influencers, track their social media platform, follower count, and campaign status.

The automated test suite includes:
- Python unit tests
- Django model tests
- Django view and client tests

GitHub Actions is configured to automatically run the full Django test suite every time code is pushed to the repository.# influencer-project
